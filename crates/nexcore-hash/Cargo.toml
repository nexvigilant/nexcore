
[package]
name = "nexcore-hash"
version = "0.1.0"
edition = "2024"
license = "MIT OR Apache-2.0"
repository.workspace = true
authors = ["NexVigilant Team"]
description = "Zero-dependency cryptographic hashing (SHA-256, HMAC-SHA256) for NexCore ecosystem"
rust-version = "1.85"

[lints]
workspace = true
