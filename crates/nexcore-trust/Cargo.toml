
[package]
name = "nexcore-trust"
version = "0.1.0"
edition = "2024"
license = "MIT OR Apache-2.0"
repository.workspace = true
homepage.workspace = true
description = "Bayesian trust engine: Beta(alpha, beta) scoring, asymmetric evidence weighting, temporal decay, multi-dimensional trust (Mayer et al.), and network transitivity"

keywords = ["nexcore", "mathematics", "science"]
categories = ["science", "algorithms"]
[dependencies]
nexcore-flywheel = { version = "0.1.0", path = "../nexcore-flywheel" }
nexcore-lex-primitiva = { version = "0.1.0", path = "../nexcore-lex-primitiva" }
stem-math = { version = "0.1.0", path = "../stem-math" }

[lints]
workspace = true
