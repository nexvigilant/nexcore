
[package]
name = "nexcore-telemetry-core"
version = "1.0.0"
edition = "2024"
authors = ["NexVigilant Team <team@nexvigilant.com>"]
license = "MIT OR Apache-2.0"
repository = "https://github.com/nexvigilant/nexcore"
rust-version = "1.85"
description = "Core telemetry aggregation and source monitoring for NexVigilant Core"
keywords = ["telemetry", "monitoring", "aggregation", "sources"]
categories = ["development-tools"]

[dependencies]
nexcore-error = { workspace = true }
serde = { version = "1.0.204", features = ["derive"] }
serde_json = { version = "1.0.120" }
nexcore-chrono = { workspace = true }
nexcore-id = { version = "0.1.0", path = "../nexcore-id", features = ["serde"] }
nexcore-lex-primitiva = { version = "0.1.0", path = "../nexcore-lex-primitiva" }
nexcore-fs = { workspace = true }
similar = "2.5"

[dev-dependencies]
tempfile = { version = "3.10.1" }


[lints]
workspace = true
