{
  "name": "nexvigilant-terminal-frontend",
  "version": "0.1.0",
  "private": true,
  "description": "NexVigilant Terminal frontend — xterm.js with epistemic color processing",
  "scripts": {
    "build": "esbuild src/main.ts --bundle --format=iife --outfile=dist/app.js --minify --sourcemap",
    "dev": "esbuild src/main.ts --bundle --format=iife --outfile=dist/app.js --sourcemap --watch",
    "typecheck": "tsc --noEmit"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@xterm/addon-fit": "^0.11.0",
    "@xterm/addon-unicode11": "^0.9.0",
    "@xterm/addon-webgl": "^0.19.0",
    "@xterm/xterm": "^6.0.0"
  },
  "devDependencies": {
    "esbuild": "^0.27.3",
    "typescript": "^5.9.3"
  }
}
