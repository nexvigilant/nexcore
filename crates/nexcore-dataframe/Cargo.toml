[package]
name = "nexcore-dataframe"
version = "0.1.0"
edition = "2024"
license = "MIT OR Apache-2.0"
repository.workspace = true
homepage.workspace = true
authors = ["NexVigilant Team"]
description = "Sovereign columnar DataFrame engine — zero-unsafe, zero-external-dependency replacement for polars"
rust-version = "1.85"

keywords = ["nexcore", "database", "storage"]
categories = ["database"]
[dependencies]
nexcore-error = { path = "../nexcore-error", version = "0.1.0" }
serde = { workspace = true }
serde_json = { workspace = true }

[dev-dependencies]
tempfile = { workspace = true }

[lints]
workspace = true
