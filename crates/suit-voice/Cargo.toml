[package]
name = "suit-voice"
version = "0.1.0"
edition = "2024"
authors = ["NexVigilant Team <team@nexvigilant.com>"]
description = "Voice Agent system — ASR, TTS, LLM routing"

[dependencies]
nexcore-error = { workspace = true }
serde = { workspace = true, features = ["derive"] }
tracing = { workspace = true }

[lints]
workspace = true
