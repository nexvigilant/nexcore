
[package]
name = "skill-macros"
version = "0.1.0"
edition = "2024"
description = "Proc macros for Claude Code skills"
license = "MIT"

[lib]
proc-macro = true

[dependencies]
proc-macro2 = "1.0"
quote = "1.0"
syn = { version = "2.0", features = ["full", "parsing"] }
skill-macros-core = { version = "1.0.0", path = "../skill-macros-core", registry = "nexcore"}
[lints]
workspace = true
