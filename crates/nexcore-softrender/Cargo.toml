
[package]
name = "nexcore-softrender"
version = "1.0.0"
edition = "2024"
license = "MIT OR Apache-2.0"
repository.workspace = true
description = "Pure-Rust software rasterizer — first-principles rendering from math to pixels"

[dependencies]

[lints]
workspace = true
