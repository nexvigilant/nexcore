
[package]
name = "nexcore-skill-exec"
version = "1.0.0"
edition = "2024"
authors = ["NexVigilant Team <team@nexvigilant.com>"]
license = "MIT OR Apache-2.0"
repository.workspace = true
homepage.workspace = true
description = "Skill Execution Engine - Real execution of Claude Code skills via shell, binary, or library invocation"
keywords = ["skills", "execution", "orchestration", "automation"]
categories = ["command-line-utilities"]

[dependencies]
nexcore-error = { workspace = true }
nexcore-lex-primitiva = { version = "0.1.0", path = "../nexcore-lex-primitiva" }
serde = { version = "1.0.204", features = ["derive"] }
serde_json = { version = "1.0.120" }
tokio = { version = "1.38.0", features = ["full"] }
tracing = { version = "0.1.40" }

# Async trait support
async-trait = "0.1"

# JSON Schema validation
jsonschema = "0.29"
nexcore-fs = { workspace = true }

[dev-dependencies]
tempfile = { version = "3.10.1" }
tokio-test = "0.4"


[lints]
workspace = true
