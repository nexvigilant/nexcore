[package]
name = "nexcore-cognition"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
description = "Typed cognitive engine — transformer algorithm as strict Rust. Attention, transformation, generation, and self-measurement."

[dependencies]
rand = { workspace = true }
thiserror = { workspace = true }
