
[package]
name = "nexcore-phenotype"
version = "1.0.0"
edition = "2024"
rust-version = "1.85"
license = "MIT OR Apache-2.0"
repository.workspace = true
authors = ["NexVigilant Team <team@nexvigilant.com>"]
description = "Adversarial test fixture generator — produces schema mutations for drift detection testing"

[dependencies]
nexcore-error = { workspace = true }
nexcore-lex-primitiva = { version = "0.1.0", path = "../nexcore-lex-primitiva" }
nexcore-transcriptase = { version = "1.0.0", path = "../nexcore-transcriptase" }
nexcore-ribosome = { version = "1.0.0", path = "../nexcore-ribosome" }
serde = { version = "1.0.204", features = ["derive"] }
serde_json = { version = "1.0.120" }


[lints]
workspace = true
