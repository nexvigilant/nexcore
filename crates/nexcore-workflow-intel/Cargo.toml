[package]
name = "nexcore-workflow-intel"
version = "0.1.0"
edition = "2024"
description = "Workflow intelligence — map workflows, identify gaps, improve live"
license = "SEE LICENSE IN LICENSE"
publish = false

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
rusqlite = { workspace = true }
chrono = { workspace = true }

[dev-dependencies]
tempfile = { workspace = true }
