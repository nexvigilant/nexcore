
[package]
name = "nexcore-compositor"
version = "0.1.0"
edition = "2024"
rust-version = "1.85"
description = "NexCore Compositor — display server for watch, phone, and desktop"
authors = ["Matthew Campion, PharmD <matthew@nexvigilant.com>"]
license = "MIT OR Apache-2.0"
repository = "https://github.com/nexvigilant/nexcore"
homepage.workspace = true
keywords = ["compositor", "display-server", "window-manager", "rendering"]
categories = ["rendering", "gui"]

[dependencies]
nexcore-pal = { version = "0.1.0", path = "../nexcore-pal" }
nexcore-lex-primitiva = { version = "0.1.0", path = "../nexcore-lex-primitiva" }
serde = { version = "1.0.204", features = ["derive"] }
serde_json = { version = "1.0.120" }
tracing = { version = "0.1.40" }

[dev-dependencies]
nexcore-pal-linux = { version = "0.1.0", path = "../nexcore-pal-linux" }


[lints]
workspace = true
