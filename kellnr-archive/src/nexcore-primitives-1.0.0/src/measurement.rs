//! # Measurement Primitives
//!
//! Re-exported from `nexcore-constants` — the single source of truth.
//! Every computed value must carry its confidence (Codex IX).

pub use nexcore_constants::{Confidence, Measured};
