[package]
name = "nucli"
version = "0.1.0"
edition = "2024"
rust-version = "1.85"
description = "Nucleotide text encoder: bijective byte↔DNA mapping with exhaustive proofs."

[dependencies]

[[bin]]
name = "nucli"
path = "src/main.rs"
