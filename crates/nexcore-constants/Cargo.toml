
[package]
name = "nexcore-constants"
version = "1.0.0"
edition = "2024"
rust-version = "1.85"
description = "T1/T2-P bedrock types: Confidence, Measured, Tier, Correction"
license = "MIT OR Apache-2.0"
repository.workspace = true

[dependencies]
serde = { version = "1.0.204", features = ["derive"] }
nexcore-lex-primitiva = { version = "0.1.0", path = "../nexcore-lex-primitiva" }

[dev-dependencies]
serde_json = { version = "1.0.120" }

[lints]
workspace = true
